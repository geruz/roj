'use strict';

import React, {Component} from 'react';
import ReactDOM from 'react-dom';

const  Statistics  = props => <div>Hello!</div>

module.exports = (init, element) => {
    ReactDOM.render(<Statistics />, element);
};
