'use strict';

const {rojModule, defaultWindowFactory, defaultDocumentFactory, setEngine} = require('./react');

module.exports = {
    rojModule,
    defaultWindowFactory,
    defaultDocumentFactory,
    setEngine,
};
